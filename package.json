{
  "name": "next-version-action",
  "version": "1.0.0",
  "description": "GitHub custom action to get the next version number with build number based on tags.",
  "main": "index.js",
  "scripts": {
    "build": "tsc -p tsconfig.build.json && ncc build build/main.js -o dist",
    "test": "jest",
    "start": "ts-node src/main.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "@types/node": "^20.14.8",
    "typescript": "^5.5.2"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@vercel/ncc": "^0.38.1",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.5",
    "ts-node": "^10.9.2"
  }
}
